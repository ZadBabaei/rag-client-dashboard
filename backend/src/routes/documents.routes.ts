// TODO: Task 4 — Document routes
// - POST /api/documents/upload — accept multipart file + clientId, trigger ingestion pipeline
// - GET /api/documents — list documents, optional ?clientId= filter
// - GET /api/documents/:id — document detail with chunks

export {};
