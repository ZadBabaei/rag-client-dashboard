// TODO: Task 3 — Client CRUD routes
// - GET /api/clients — list clients, optional ?search= filter
// - GET /api/clients/:id — get client by id with documents
// - POST /api/clients — create new client (name, email?, phone?)

export {};
