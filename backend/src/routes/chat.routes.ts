// TODO: Task 6 — Chat/RAG route
// - POST /api/chat — accept { question, clientId?, conversationId?, stream? }
// - Orchestrate: embed query → vector search → rerank → assemble context → LLM call
// - Return { answer, citations[], conversationId }
// - Task 7: Add SSE streaming support
// - Task 15: Add conversation memory

export {};
