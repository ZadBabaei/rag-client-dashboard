// TODO: Task 2 — Express app setup
// - Import config (validates env vars on startup)
// - Create Express app with CORS, JSON body parser
// - Mount routes: /api/clients, /api/documents, /api/chat, /api/eval
// - Global error handler middleware
// - GET /health endpoint
// - Listen on config.PORT

export {};
