// TODO: Task 5 — Ingestion orchestrator
// - ingestDocument(file: { buffer: Buffer, originalname: string, mimetype: string, size: number }, clientId: string): Promise<DocumentRecord>
// - Steps: parse → chunk → embed all chunks → store Document + DocumentChunks in DB
// - Task 8: Also trigger profile extraction after ingestion

export {};
