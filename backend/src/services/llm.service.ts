// TODO: Task 6 — LLM generation service
// - generate(context: AssembledContext): Promise<string> — non-streaming generation
// - generateStream(context: AssembledContext): AsyncGenerator<string> — streaming tokens
// - Supports OpenAI GPT-4o and Anthropic Claude (configurable via LLM_PROVIDER)
// - System prompt instructs LLM to include citations as [Source: doc_name, page X]

export {};
