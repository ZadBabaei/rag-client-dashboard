// TODO: Task 6 — Vector similarity search
// - searchSimilar(embedding: number[], options: { clientId?: string, limit?: number }): Promise<RetrievedChunk[]>
// - Uses pgvector cosine distance operator (<=>)
// - Returns top N chunks with similarity scores
// - Optionally filters by clientId

export {};
