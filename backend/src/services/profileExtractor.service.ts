// TODO: Task 8 — Client profile extraction
// - extractProfile(text: string): Promise<UpdateClientProfileRequest>
// - Uses LLM with structured prompt to extract: AUM, riskTolerance, goals, familyMembers
// - Returns partial profile (only fields found in text)
// - Handles cases where fields are not present

export {};
